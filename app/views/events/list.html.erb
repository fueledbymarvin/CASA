	<div id="aboutheader">
		<div class="slidercover"><img class="slider" src="<%= asset_path 'events header.png' %>"></div>
		<div class="circle"><img src="<%= asset_path 'events circle.png' %>"></div>
	</div>

	<div id="aboutcontent">
		<div id="eventheader">
			<h1 class="eventtype">All</h1>
			<h1 class="eventtype">News</h1>
			<h1 class="eventtype">Social</h1>
			<h1 class="eventtype">Cultural</h1>
			<h1 class="eventtype">Community</h1>
			<h1 class="eventtype">Political</h1>

			<div id="eventnav">
				<p class="eventbutton">All</p>
				<p class="eventbutton">News</p>
				<p class="eventbutton">Social</p>
				<p class="eventbutton">Cultural</p>
				<p class="eventbutton">Community</p>
				<p class="eventbutton">Political</p>
			</div>
		</div>

		<div id="eventcontainer">
			<div id="eventcontent">
				<div class="eventsection">
					<% @events.each do |event| %>
				        <div class="post">
				            <div class="posticon">
				                <img class="lantern" src="<%= asset_path 'lantern.png' %>" alt="Lantern">
				                <h3 class="date"><%= event.day.strftime("%b").upcase %><br /><%= event.day.strftime("%d") %></h3>
				            </div>
				            
				            <div class="posttitles">
				                <h1 class="title"><%= link_to event.title.html_safe, event %></h1>
				                <p class="subtitle">
				                    <% if event.hassub %>
				                        <%= event.subtitle.html_safe %>
				                    <% else %>
				                        <% if event.addend %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p").downcase + " - " + event.endtime.strftime("%l:%M%p.").downcase %>
				                        <% else %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p.").downcase %>
				                        <% end %>
				                        <%= event.location + "." %>
				                    <% end %>
				                </p>
				            </div>

				          	<% if event.photo? %>
					          <div class="postphoto"><a href="<%= event.photo.url %>"><%= image_tag event.photo.url(:display) %></a></div>
					        <% end %>

				            <p class="postcontent"><%= event.info.html_safe %></p>
				            <p class="postbottom">posted under <a href="list?=<%= event.category %>"><%= event.category %></a></p>
				        </div>
				    <% end %>
				</div>
				<div class="eventsection">
					<% @events.each do |event| %>
						<% if event.category == "News" %>
				        <div class="post">
				            <div class="posticon">
				                <img class="lantern" src="<%= asset_path 'lantern.png' %>" alt="Lantern">
				                <h3 class="date"><%= event.day.strftime("%b").upcase %><br /><%= event.day.strftime("%d") %></h3>
				            </div>
				            
				            <div class="posttitles">
				                <h1 class="title"><%= link_to event.title.html_safe, event %></h1>
				                <p class="subtitle">
				                    <% if event.hassub %>
				                        <%= event.subtitle.html_safe %>
				                    <% else %>
				                        <% if event.addend %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p").downcase + " - " + event.endtime.strftime("%l:%M%p.").downcase %>
				                        <% else %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p.").downcase %>
				                        <% end %>
				                        <%= event.location + "." %>
				                    <% end %>
				                </p>
				            </div>

				            <% if event.photo? %>
					          <div class="postphoto"><a href="<%= event.photo.url %>"><%= image_tag event.photo.url(:display) %></a></div>
					        <% end %>

				            <p class="postcontent"><%= event.info.html_safe %></p>
				            <p class="postbottom">posted under <a href="list?=<%= event.category %>"><%= event.category %></a></p>
				        </div>
				        <% end %>
				    <% end %>
				</div>
				<div class="eventsection">
					<% @events.each do |event| %>
						<% if event.category == "Social" %>
				        <div class="post">
				            <div class="posticon">
				                <img class="lantern" src="<%= asset_path 'lantern.png' %>" alt="Lantern">
				                <h3 class="date"><%= event.day.strftime("%b").upcase %><br /><%= event.day.strftime("%d") %></h3>
				            </div>
				            
				            <div class="posttitles">
				                <h1 class="title"><%= link_to event.title.html_safe, event %></h1>
				                <p class="subtitle">
				                    <% if event.hassub %>
				                        <%= event.subtitle.html_safe %>
				                    <% else %>
				                        <% if event.addend %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p").downcase + " - " + event.endtime.strftime("%l:%M%p.").downcase %>
				                        <% else %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p.").downcase %>
				                        <% end %>
				                        <%= event.location + "." %>
				                    <% end %>
				                </p>
				            </div>

				            <% if event.photo? %>
					          <div class="postphoto"><a href="<%= event.photo.url %>"><%= image_tag event.photo.url(:display) %></a></div>
					        <% end %>

				            <p class="postcontent"><%= event.info.html_safe %></p>
				            <p class="postbottom">posted under <a href="list?=<%= event.category %>"><%= event.category %></a></p>
				        </div>
				        <% end %>
				    <% end %>
				</div>
				<div class="eventsection">
					<% @events.each do |event| %>
						<% if event.category == "Cultural" %>
				        <div class="post">
				            <div class="posticon">
				                <img class="lantern" src="<%= asset_path 'lantern.png' %>" alt="Lantern">
				                <h3 class="date"><%= event.day.strftime("%b").upcase %><br /><%= event.day.strftime("%d") %></h3>
				            </div>
				            
				            <div class="posttitles">
				                <h1 class="title"><%= link_to event.title.html_safe, event %></h1>
				                <p class="subtitle">
				                    <% if event.hassub %>
				                        <%= event.subtitle.html_safe %>
				                    <% else %>
				                        <% if event.addend %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p").downcase + " - " + event.endtime.strftime("%l:%M%p.").downcase %>
				                        <% else %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p.").downcase %>
				                        <% end %>
				                        <%= event.location + "." %>
				                    <% end %>
				                </p>
				            </div>

				            <% if event.photo? %>
					          <div class="postphoto"><a href="<%= event.photo.url %>"><%= image_tag event.photo.url(:display) %></a></div>
					        <% end %>

				            <p class="postcontent"><%= event.info.html_safe %></p>
				            <p class="postbottom">posted under <a href="list?=<%= event.category %>"><%= event.category %></a></p>
				        </div>
				        <% end %>
				    <% end %>
				</div>
				<div class="eventsection">
					<% @events.each do |event| %>
						<% if event.category == "Community" %>
				        <div class="post">
				            <div class="posticon">
				                <img class="lantern" src="<%= asset_path 'lantern.png' %>" alt="Lantern">
				                <h3 class="date"><%= event.day.strftime("%b").upcase %><br /><%= event.day.strftime("%d") %></h3>
				            </div>
				            
				            <div class="posttitles">
				                <h1 class="title"><%= link_to event.title.html_safe, event %></h1>
				                <p class="subtitle">
				                    <% if event.hassub %>
				                        <%= event.subtitle.html_safe %>
				                    <% else %>
				                        <% if event.addend %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p").downcase + " - " + event.endtime.strftime("%l:%M%p.").downcase %>
				                        <% else %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p.").downcase %>
				                        <% end %>
				                        <%= event.location + "." %>
				                    <% end %>
				                </p>
				            </div>

				            <% if event.photo? %>
					          <div class="postphoto"><a href="<%= event.photo.url %>"><%= image_tag event.photo.url(:display) %></a></div>
					        <% end %>

				            <p class="postcontent"><%= event.info.html_safe %></p>
				            <p class="postbottom">posted under <a href="list?=<%= event.category %>"><%= event.category %></a></p>
				        </div>
				        <% end %>
				    <% end %>
				</div>
				<div class="eventsection">
					<% @events.each do |event| %>
						<% if event.category == "Political" %>
				        <div class="post">
				            <div class="posticon">
				                <img class="lantern" src="<%= asset_path 'lantern.png' %>" alt="Lantern">
				                <h3 class="date"><%= event.day.strftime("%b").upcase %><br /><%= event.day.strftime("%d") %></h3>
				            </div>
				            
				            <div class="posttitles">
				                <h1 class="title"><%= link_to event.title.html_safe, event %></h1>
				                <p class="subtitle">
				                    <% if event.hassub %>
				                        <%= event.subtitle.html_safe %>
				                    <% else %>
				                        <% if event.addend %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p").downcase + " - " + event.endtime.strftime("%l:%M%p.").downcase %>
				                        <% else %>
				                            <%= event.day.strftime("%A, %B %e, %Y. ") + event.starttime.strftime("%l:%M%p.").downcase %>
				                        <% end %>
				                        <%= event.location + "." %>
				                    <% end %>
				                </p>
				            </div>

				            <% if event.photo? %>
					          <div class="postphoto"><a href="<%= event.photo.url %>"><%= image_tag event.photo.url(:display) %></a></div>
					        <% end %>

				            <p class="postcontent"><%= event.info.html_safe %></p>
				            <p class="postbottom">posted under <a href="list?=<%= event.category %>"><%= event.category %></a></p>
				        </div>
				        <% end %>
				    <% end %>
				</div>
			</div>
		</div>
	</div>